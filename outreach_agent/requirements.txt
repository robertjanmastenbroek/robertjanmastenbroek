# RJM Outreach Agent — Python Dependencies
# Install: pip install -r requirements.txt
#
# Note: Email generation uses the Claude Code CLI (your Max plan),
# not the Anthropic Python SDK. No API key required.

# Google Gmail API
google-auth>=2.27.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.118.0

# Video hosting — Cloudinary (set CLOUDINARY_URL in Railway env vars)
cloudinary>=1.40.0

# BTL (Boil the Lake) protocol — Thompson Sampling bandits
numpy>=1.24.0

# BTL self-funding loop — Stripe donation polling (optional; poll_stripe gracefully
# degrades if the package is missing or STRIPE_API_KEY is unset)
stripe>=7.0.0
